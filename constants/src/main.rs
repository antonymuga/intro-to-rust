fn main() {
    const MAX_LANGS:i32 = 5; // constants cant be the result of runtime compute
    const MULTIPLIER:f32 = 3.14; // data type declaration for constants is a must
    println!("Maximum programming langauges you can learn is {}", MAX_LANGS);
    println!("The salary multiplier is {}", MULTIPLIER);
}
